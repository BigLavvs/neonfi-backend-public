// Neonfi backend — Token Metadata Sync job (PLACEHOLDER).
// Real implementation: Stage 9. Populates the `token` table on a schedule and
// invalidates the Redis token-metadata cache.
// NOTE: the metadata vendor is an OPEN decision (Appendix item 2 — Moralis is
// primary; CoinMarketCap / CoinRanking under evaluation). Do NOT hardcode a
// vendor here.
export {};
